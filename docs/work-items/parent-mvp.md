Deliver an installable MVP of the Figma responsive Auto Layout refactor plugin defined in `docs/PRD.md`.

The developer leader owns coordination. Inspect stage results, promote only dependency-ready backlog issues, and keep the parent in progress until all required stages are complete. Do not treat dispatch as completion.

Required outcome: buildable plugin source, deterministic responsive inference, safe duplicate output, Wrap, Min/Max, breakpoint variants, validation report, automated tests, Golden Fixtures, setup documentation, and a final QA go/no-go report.

